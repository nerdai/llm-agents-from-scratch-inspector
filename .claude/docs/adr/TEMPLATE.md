<!--
How to use this template
=========================
1. Copy this file to a new ADR in the same directory (docs/adr/).
2. Name it: ADR-0xx-<short-kebab-case-description>.md
   - `0xx` is the next zero-padded sequence number (ADR-001, ADR-002, ...);
     never reuse or renumber once an ADR has merged, even if it's later superseded.
   - `<desc>` is a short kebab-case slug of the title, e.g. `fastapi-service-layering`.
   - Example: docs/adr/ADR-001-fastapi-service-layering.md
3. Delete this comment block from the copy.
4. Fill in every section below. Keep "Status" current as the decision evolves.
-->

# ADR-0xx: <Short, specific title stating the decision, not the problem>

## Status

Proposed <!-- Proposed | Accepted | Rejected | Deprecated | Superseded by ADR-0yy -->

Date: YYYY-MM-DD

## Context

What is the issue we're seeing that motivates this decision or change? State the
forces at play — technical, business, team — as neutrally as possible. Include
enough background that someone with no prior context can follow the reasoning.

## Decision

What is the change we're actually proposing or have agreed to? State it as a
clear, active statement: "We will ...". This is the part people will skim first.

## Alternatives considered

What other options were on the table, and why were they not chosen? Even a
one-line dismissal per alternative is enough — the goal is to show the decision
wasn't made in a vacuum and to save future readers from re-litigating a path
already ruled out.

- **Alternative A** — why it was passed over.
- **Alternative B** — why it was passed over.

## Consequences

What becomes easier or harder as a result of this change? Include both the
positive and the negative — every decision has tradeoffs. Note any follow-up
work this creates (new issues, migrations, docs to update).

## References

Links to related ADRs, issues, PRs, or external docs that informed this
decision.
